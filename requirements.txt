# FastAPI and dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Additional production dependencies
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# OCR Dependencies
google-genai>=1.0.0
Pillow
PyPDF2
pdf2image
pymupdf

# Chat/LLM Dependencies
openai>=1.0.0
groq>=0.4.0
requests>=2.31.0
httpx>=0.24.0
tiktoken>=0.5.0
transformers>=4.40.0,<5.0.0

# RAG Dependencies
pinecone>=3.0.0
sentence-transformers>=3.0.0,<4.0.0
torch>=2.0.0

# AWS S3 Dependencies
boto3>=1.28.0
botocore>=1.31.0

# Magic Tools Dependencies (for file content extraction)
python-docx>=1.1.0  # For .docx files
python-pptx>=0.6.23  # For .pptx files
openpyxl>=3.1.0  # For Excel files (.xlsx, .xls)

# Meeting Summary Dependencies (for video/audio processing)
moviepy>=1.0.3  # For video to audio conversion
static-ffmpeg>=2.0  # Bundled ffmpeg binary (no system installation needed)
imageio>=2.9.0  # Required by moviepy
imageio-ffmpeg>=0.4.0  # FFmpeg backend for imageio
decorator>=4.0.0  # Required by moviepy
proglog>=0.1.9  # Progress logging for moviepy
numpy>=1.17.0  # Required by moviepy

